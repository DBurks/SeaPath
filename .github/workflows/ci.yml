name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
        build_type: [Release, Debug]

    steps:
      - name: checkout code
        uses: actions/checkout@v3

      # Install lcov only on Ubuntu for the Debug coverage pass
      - name: Install LCOV (Ubuntu Debug only)
        if: matrix.os == 'ubuntu-latest' && matrix.build_type == 'Debug'
        run: |
          sudo apt-get update
          sudo apt-get install -y lcov

      - name: Create Build Directory
        run: mkdir build

      # Dynamically inject coverage flags only for Ubuntu Debug runs
      - name: Configure CMake
        run: |
          cd build
          if [ "${{ matrix.os }}" = "ubuntu-latest" ] && [ "${{ matrix.build_type }}" = "Debug" ]; then
            cmake .. -DCMAKE_BUILD_TYPE=${{ matrix.build_type }} -DCMAKE_CXX_FLAGS="--coverage" -DCMAKE_EXE_LINKER_FLAGS="--coverage"
          else
            cmake .. -DCMAKE_BUILD_TYPE=${{ matrix.build_type }}
          fi

      - name: Build
        run: cd build && cmake --build . --config ${{ matrix.build_type }}

      # Run Tests: Uses native binary for Ubuntu Debug to drop data, preserves ctest for macOS/Release
      - name: Run Tests
        run: |
          cd build
          if [ "${{ matrix.os }}" = "ubuntu-latest" ] && [ "${{ matrix.build_type }}" = "Debug" ]; then
            ./tests/seapath_tests
          else
            ctest --output-on-failure -C ${{ matrix.build_type }}
          fi

      # Process and extract lcov metrics safely from within the build directory
      - name: Generate and Filter Coverage Report (Ubuntu Debug only)
        if: matrix.os == 'ubuntu-latest' && matrix.build_type == 'Debug'
        run: |
          cd build
          
          # 1. Capture data using the exact local directory layout
          lcov --directory . --base-directory .. --capture --output-file coverage.info --ignore-errors mismatch,unused
          
          # 2. Filter out system headers and unit tests cleanly
          lcov --remove coverage.info '/usr/*' '*/_deps/*' '*/tests/*' --output-file coverage.cleaned.info --ignore-errors mismatch,unused
          
          # 3. Generate the static HTML report artifact matching your local workspace
          genhtml coverage.cleaned.info --output-directory coverage_report

      # Zip and upload the HTML dashboard directly to your GitHub Action run panel
      - name: Upload Coverage Artifacts
        if: matrix.os == 'ubuntu-latest' && matrix.build_type == 'Debug'
        uses: actions/upload-artifact@v4
        with:
          name: seapath-lcov-report
          path: build/coverage_report/